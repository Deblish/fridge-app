<!-- views/partials/bottom-nav.ejs -->
<nav class="bg-white shadow fixed bottom-0 left-0 right-0">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-center h-16">
      <% if (typeof bottomNav !== 'undefined' && bottomNav === 'add') { %>
        <a href="/" class="px-4 py-2 text-indigo-600 hover:text-indigo-800">Add Item</a>
      <% } else if (typeof bottomNav !== 'undefined' && bottomNav === 'my-items') { %>
        <a href="/my-items" class="px-4 py-2 text-indigo-600 hover:text-indigo-800">My Items</a>
      <% } %>
    </div>
  </div>
</nav>

